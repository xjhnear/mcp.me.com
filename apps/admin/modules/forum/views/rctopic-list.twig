<!--{%extends "sub-layout-forum.twig"%}-->
<!--{%block main_content%}-->
<div class="row-fluid">
	<div class="span12">
		<div class="box box-color box-bordered">
			<div class="box-title">
				<h3>
					<i class="icon-table"></i>
					Web社区推荐帖子
				</h3>
				<div class="actions">
					<table class="table-filter">
					<tbody><tr>
						<td><a class="btn btn-teal" href="<!--{{ url('/forum/bbs/add-rctopic') }}-->">添加</a></td>
					</tr>
					</tbody></table>
				</div>
			</div>
			<div class="box-content nopadding">
				<table class="table table-hover table-nomargin">
					<thead>
					<tr>
						<th class="col-50">ID</th>
						<th class="col-50">帖子ID</th>
						<th class="col-150">标题</th>
						<th class="col-150">短标题</th>
						<th class="col-150">地址</th>
						<th class="col-60">排序</th>
						<th class="col-100">添加时间</th>
						<th class="col-80">是否显示</th>
						<th class="table-btn-group">操作</th>
					</tr>
					</thead>
					<tbody>
					<!--{%for item in topics%}-->
					<tr>
						<td><!--{{item.bbs_recommend_id}}--></td>
						<td><!--{{item.topic_id}}--></td>
						<td><!--{{item.topic_title}}--></td>
						<td><!--{{item.topic_short}}--></td>
						<td><!--{{ item.action_url }}--></td>
						<td><!--{{ item.sort }}--></td>
						<td><!--{{ item.add_time|date('Y-m-d H:i:s') }}--></td>
						<td><!--{% if item.is_show == 1 %}-->显示<!--{% else %}-->隐藏<!--{% endif %}--></td>
						<td>
							<a href="<!--{{url('forum/bbs/edit-rctopic',item.bbs_recommend_id)}}-->" class="btn btn-primary">编辑</a>
							<a href="javascript:void(0);" data="<!--{{ item.bbs_recommend_id }}-->" class="btn btn-primary ajax-del">删除</a>
						</td>
					</tr>
					<!--{%endfor%}-->
					</tbody>
				</table>
				<div class="pagelink">
					<!--{{ pagination }}-->
				</div>
			</div>
		</div>
	</div>
</div>
<!--{%endblock%}-->

<!--{%block footer_js%}-->
<script>
	$(function(){
		$('a.ajax-del').bind('click',function(){
			var recommend_id = $(this).attr('data');
			alertify.confirm('确定删除数据么？',function(e){
				if(e){
					$.get('/forum/bbs/del-rctopic/'+recommend_id,{},function(resp){
						if(resp.state){
							alertify.success(resp.msg);
							setTimeout(function(){
								location.reload();
							},2000);
						}else{
							alertify.error(resp.msg);
						}
					},'json')
				}
			});
		});
	});
</script>
<!--{%endblock%}-->