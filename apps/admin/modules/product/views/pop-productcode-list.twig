<div class="box">
    <div class="actions">
        <table class="table-filter">
	        <tr>				            
	            <td>
	                <div class="controls">
					   <label class="radio inline"> 名称</label>
					</div>				                
	            </td>
	            <td>
					<input id="keyword" type="text" class="input-medium" placeholder="" value="<!--{{keyword}}-->">	
                    </td>
				<td>
				    <a href="javascript:searchCate();" class="btn btn-teal">按条件搜索</a>
				</td>
			</tr>
		</table>
    </div>    
    <table class="table table-hover table-nomargin">
        <thead>
			<tr>
			     <th class="col-150">名称</th>
                 <th class="col-150">起止时间</th>                           
                 <th class="col-80">所需游币</th>
                 <th class="col-120">剩余量/总量</th>
                 <th class="col-80">选项</th>
                 <th class="col-60">状态</th>
				 <th class="table-btn-group" style="width:50px;">操作</th>
			</tr>
		</thead>
		<tbody>
		    <!--{%for item in datalist%}-->
			<tr>
                <td id="product_name_<!--{{item.productCode}}-->"><!--{{item.title}}--></td>
                <td>
                    <!--{% if item.activity %}--> 
                             <!--{{item.activity.startTimeStr|date('Y-m-d')}}-->至<!--{{item.activity.endTimeStr|date('Y-m-d')}}-->
                    <!--{% else %}--> 
                              没有活动
                    <!--{% endif %}--> 
                </td>                           
                <td><!--{{item.productGamePrice}}--></td>
                <td><!--{{item.restCount}}-->/<!--{{item.totalCount}}--></td>
                <td>
                     <!--{%if item.singleLimit==0%}-->不限制<!--{%else%}--><!--{{item.singleLimit}}--><!--{%endif%}-->
                </td>
                
                <td>
                     <!--{%if item.isOnshelf==1%}--><span class="badge badge-success">上架</span><!--{%else%}--><span class="badge badge-important">下架</span><!--{%endif%}-->
                 </td>		
				 <td>
				    <a href="javascript:void(0);" data-id="<!--{{item.productCode}}-->" class="btn btn-primary select-cate-id">选择</a>				    							    
				</td>
                
                
              </tr>
			<!--{%endfor%}-->
		</tbody>
    </table>
    <div class="pagelink">
        <!--{{pagelinks}}-->
    </div>
</div>    
<script>
    function searchCate()
    {
        //var keytype = $("input[name='keytype']:checked").val();
        var keyword = $('#keyword').val();
        $.ajax({
            url:'/product/goods/product-code',
            data:{keyword:keyword},
            dataType:'json',
            success:function(json){
                var html = '';
                html = json.html;
                $.dialog.list['pop-activity-list'].content(html);		                
            }
        });
    }
    function init_page()
    {
        $('.select-cate-id').each(function(){
            $(this).click(function(){
                var product_code = $(this).attr('data-id'),product_name = $('#product_name_'+product_code).text();
                set_selected_cate(product_code,product_name);
                $.dialog.list['pop-activity-list'].close();
            });
        });
        $('.pagelink a').each(function(){
            var url = $(this).attr('href'); 
            $(this).attr('href','javascript:void(0);');
            $(this).click(function(){
                $.ajax({
                    url:url,
                    dataType:'json',
		            success:function(json){
		                var html = '';
		                html = json.html;
		                $.dialog.list['pop-activity-list'].content(html);		                
		            }
                });
            });
            
        });
        //:
        $('.aui_main').css({"display":"inline-block"});
    }
    init_page();
</script>