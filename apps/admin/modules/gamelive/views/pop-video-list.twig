<div class="box">
    <div class="actions">
        <table class="table-filter">
            <tr>
                <td>视频</td>
                <td>
                    <input id="keyword" type="text" class="input-medium" placeholder="" value="<!--{{keyword}}-->">
                </td>
                <td>
                    <a href="javascript:searchvideo();" class="btn btn-teal">按条件搜索</a>
                </td>
            </tr>
        </table>
    </div>
    <table class="table table-hover table-nomargin">
        <thead>
        <tr>
            <th class="col-60">ID</th>
            <th class="col-80">图标</th>
            <th class="col-150">名字</th>
            <th class="table-btn-group col-60">操作</th>
        </tr>
        </thead>
        <tbody>
        <!--{%for item in datalist%}-->
        <tr>
            <td><!--{{item.id}}--></td>
            <td class="user-avatar"><img id="video_icon_<!--{{item.id}}-->" src="<!--{{item.titlePic}}-->" class="" /></td>
            <td id="video_name_<!--{{item.id}}-->"><!--{{item.title}}--></td>
            <td>
                <a href="javascript:void(0);" data-id="<!--{{item.id}}-->" data-name="<!--{{item.title}}-->" class="btn btn-primary select-video-id">选择</a>
            </td>
        </tr>
        <!--{%endfor%}-->
        </tbody>
    </table>
    <div class="pagelink">
        <!--{{pagelinks}}-->
    </div>
</div>
<script>
    function searchGame()
    {
        var keyword = $('#keyword').val();
        $.ajax({
            url:'/gamelive/api/pop-video-search',
            data:{keyword:keyword},
            dataType:'json',
            success:function(json){
                var html = '';
                html = json.html;
                $.dialog.list['pop-video-list'].content(html);
            }
        });
    }
    function init_page()
    {
        //
        $('.select-video-id').each(function(){
            $(this).click(function(){
                var video_id = $(this).attr('data-id');
                var video_name = $(this).attr('data-name');
                set_selected_video(video_id,video_name);
                $.dialog.list['pop-video-list'].close();
            });
        });
        //
        $('.pagelink a').each(function(){
            var url = $(this).attr('href');
            $(this).attr('href','javascript:void(0);');
            $(this).click(function(){
                $.ajax({
                    url:url,
                    dataType:'json',
                    success:function(json){
                        var html = '';
                        html = json.html;
                        $.dialog.list['pop-video-list'].content(html);
                    }
                });
            });

        });
    }
    init_page();
</script>